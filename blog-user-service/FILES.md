````markdown
# Blog User Service - 文件摘要列表

本文档列出了 `blog-user-service` 微服务的核心文件及其用途，便于快速了解代码结构。

## 📁 项目结构

```
blog-user-service/
├── add-notifications-table.ps1
├── pom.xml
├── README.md
├── QUICK_REFERENCE.md
├── start.ps1
├── test-api.ps1
└── src/
    └── main/
        ├── resources/
        │   └── application.yml
        └── java/
            └── com/
                └── example/
                    └── blog/
                        └── user/
                            ├── BlogUserServiceApplication.java
                            ├── config/
                            │   └── SecurityConfig.java
                            ├── controller/
                            │   ├── NotificationController.java
                            │   └── UserController.java
                            ├── dto/
                            │   ├── CreateNotificationRequest.java
                            │   ├── NotificationDTO.java
                            │   ├── RegistrationRequest.java
                            │   ├── UpdateUsernameRequest.java
                            │   ├── UserAuthDetailsDTO.java
                            │   └── UserDTO.java
                            ├── entity/
                            │   ├── Notification.java
                            │   ├── User.java
                            │   └── UserAuth.java
                            ├── mapper/
                            │   ├── NotificationMapper.java
                            │   ├── UserAuthMapper.java
                            │   └── UserMapper.java
                            ├── service/
                            │   ├── NotificationService.java
                            │   ├── NotificationServiceImpl.java
                            │   ├── UserService.java
                            │   └── UserServiceImpl.java
                            └── util/
                                └── JwtUtil.java
```

## 📄 文件说明

### 配置类

| 文件                         | 说明                                                                                |
| ---------------------------- | ----------------------------------------------------------------------------------- |
| `config/SecurityConfig.java` | Spring Security 配置，开放用户与通知相关 API，提供 BCrypt 密码编码器，并配置 CORS。 |

### 控制器层

| 文件                                     | 说明                                                                       |
| ---------------------------------------- | -------------------------------------------------------------------------- |
| `controller/UserController.java`         | 负责注册、用户资料查询、头像与用户名修改等接口，同时暴露内部认证查询端点。 |
| `controller/NotificationController.java` | 暴露通知 CRUD API，包括内部创建端点和面向用户的查询、已读标记操作。        |

### DTO 层

| 文件                                 | 说明                                                         |
| ------------------------------------ | ------------------------------------------------------------ |
| `dto/CreateNotificationRequest.java` | 内部服务创建通知时的请求体，校验用户、文章、触发行为等字段。 |
| `dto/NotificationDTO.java`           | 通知响应对象，携带触发者头像、用户名和已读状态等信息。       |
| `dto/RegistrationRequest.java`       | 用户注册请求体，包含用户名、密码、邮箱等字段。               |
| `dto/UpdateUsernameRequest.java`     | 用户名更新请求体，带长度与非空校验。                         |
| `dto/UserDTO.java`                   | 对外展示的用户资料。                                         |
| `dto/UserAuthDetailsDTO.java`        | 内部认证详情返回对象，包含密码哈希与账户状态。               |

### 实体与数据访问层

| 文件                             | 说明                                                      |
| -------------------------------- | --------------------------------------------------------- |
| `entity/User.java`               | 映射 `t_user` 表，保存邮箱、头像、简介、状态等资料。      |
| `entity/UserAuth.java`           | 映射 `t_user_auth` 表，存储认证方式、凭证等信息。         |
| `entity/Notification.java`       | 映射 `t_user_notification` 表，记录文章互动所产生的通知。 |
| `mapper/UserMapper.java`         | User 实体的 MyBatis-Plus 基础 CRUD 接口。                 |
| `mapper/UserAuthMapper.java`     | UserAuth 实体的 MyBatis-Plus 基础 CRUD 接口。             |
| `mapper/NotificationMapper.java` | Notification 实体的 MyBatis-Plus 基础 CRUD 接口。         |

### 业务逻辑层

| 文件                                   | 说明                                                       |
| -------------------------------------- | ---------------------------------------------------------- |
| `service/UserService.java`             | 用户业务接口，定义注册、资料查询、头像与用户名更新等操作。 |
| `service/UserServiceImpl.java`         | 用户业务实现，处理密码加密、唯一性校验、头像存储等逻辑。   |
| `service/NotificationService.java`     | 通知业务接口，定义创建、查询、已读标记等操作。             |
| `service/NotificationServiceImpl.java` | 通知业务实现，生成默认提示文案并补充触发者用户名与头像。   |

### 工具与启动文件

| 文件                              | 说明                                                       |
| --------------------------------- | ---------------------------------------------------------- |
| `util/JwtUtil.java`               | 解析 JWT 并提取用户 ID，供需要认证的接口使用。             |
| `BlogUserServiceApplication.java` | Spring Boot 启动入口，启用 MyBatis Mapper 扫描与服务发现。 |

### 脚本与文档

| 文件                               | 说明                                                                 |
| ---------------------------------- | -------------------------------------------------------------------- |
| `add-notifications-table.ps1`      | PowerShell 脚本，用于在 PostgreSQL 中创建 `t_user_notification` 表。 |
| `start.ps1`                        | 一键构建并运行服务。                                                 |
| `test-api.ps1`                     | 调用关键 REST 接口的冒烟测试脚本。                                   |
| `README.md` / `QUICK_REFERENCE.md` | 使用说明、接口文档以及常用命令速查。                                 |

## 🔑 关键特性更新摘要

- 用户体系全面切换为 **username**，支持个人主页修改用户名。
- 新增通知业务：当文章被点赞或评论时，作者会收到提醒。
- 通知接口返回触发者头像与用户名，便于前端渲染。
- PowerShell 脚本可快速建表，确保与数据库结构同步。
- 所有公共输入使用 Jakarta Validation，保证数据安全可靠。
````
